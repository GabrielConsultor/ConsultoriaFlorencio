/*
  # Configuração de cabeçalhos de segurança
  
  # Impede o carregamento do site dentro de iframes (clickjacking)
  X-Frame-Options: DENY

  # Impede que o navegador interprete arquivos com tipos MIME incorretos
  X-Content-Type-Options: nosniff

  # Força o uso de HTTPS sempre, protegendo contra downgrade de protocolo
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

  # Ativa proteção extra contra XSS no navegador
  X-XSS-Protection: 1; mode=block

  # Controla quais informações de referrer são passadas, melhorando a privacidade
  Referrer-Policy: no-referrer-when-downgrade

  # Permite ou nega o uso de APIs e funcionalidades do navegador
  Permissions-Policy: geolocation=(), microphone=(), camera=()

  # Ajuda a prevenir o uso de recursos de forma não autorizada por outros sites
  Cross-Origin-Resource-Policy: same-origin

  # Protege contra o roubo de cookies e dados de popups maliciosos
  Cross-Origin-Opener-Policy: same-origin

  # Impede que o site seja incorporado em páginas de outros sites
  Cross-Origin-Embedder-Policy: require-corp

  # Política de segurança de conteúdo
  Content-Security-Policy: default-src 'self'; script-src 'self' https://apis.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: https://fonts.gstatic.com https://*.whatsapp.net https://*.whatsapp.com; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://api.whatsapp.com https://forms.netlify.com; frame-ancestors 'none'; form-action 'self' https://forms.netlify.com; upgrade-insecure-requests;